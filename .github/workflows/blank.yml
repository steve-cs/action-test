name: CI
on: 
  push:
jobs:
  job1:
    runs-on: ubuntu-18.04
    container: docker://ubuntu:18.04
    steps:
    - run: echo Hello World
    - run: touch bozo
    - run: ls -l .
    - run: touch /usr/local/bozo
    - run: ls -l /usr/local/bozo
